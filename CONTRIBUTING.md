# Contributing

This repository holds reusable GitHub Actions workflows — check and security jobs that other repositories call instead of reimplementing them.

It is maintained by one person and does not take outside contributions — changes land through a pull request against `main`, where the commit is signed and the merge is squashed.
